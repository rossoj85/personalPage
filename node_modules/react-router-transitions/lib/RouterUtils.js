'use strict';

exports.__esModule = true;
/**
 * Resolve all routes to get the complete paht.
 *
 * @param {object[]} routes
 * @returns {object}
 */
var getCompleteRoutesPath = exports.getCompleteRoutesPath = function getCompleteRoutesPath(routes) {
  return routes.reduce(function (complete, _ref) {
    var _ref$path = _ref.path,
        path = _ref$path === undefined ? '' : _ref$path;

    return path.startsWith('/') ? path : complete.endsWith('/') ? '' + complete + path : complete + '/' + path;
  }, '');
};

/**
 * Get route path.
 *
 * @param {object} options
 */
var getRoutePath = exports.getRoutePath = function getRoutePath(route, routes) {
  var currentRouteIndex = routes.indexOf(route);
  var currentRoutes = routes.slice(0, currentRouteIndex + 1);
  return getCompleteRoutesPath(currentRoutes);
};